#include <stdio.h>

int main(int argc, char *argv[]) {
    printf("Bienvenue dans le compilateur CypLang !\n");
    // we could add more functionality here, such as parsing command line arguments
    return 0;
}